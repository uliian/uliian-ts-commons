import { renderHook } from "@testing-library/react";
import useOffsetPage from "../src/hooks/useOffsetPage";

// test("useOffsetPage_first", () => {
//     const { result } = renderHook(() => useOffsetPage<any>((page)=>));
    
// })